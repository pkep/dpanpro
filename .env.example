# ── Backend Spring Gateway ──
JWT_SECRET=depanpro-super-secret-key-at-least-256-bits-change-me

# ── SMTP (emails) ──
MAIL_HOST=smtp.gmail.com
MAIL_USER=contact@depan-pro.com
MAIL_PASSWORD=

# ── Stripe ──
STRIPE_API_KEY=

# ── Grafana ──
GRAFANA_PASSWORD=depanpro123

# ── Frontend (mode API) ──
# 'supabase' (défaut Lovable Cloud) ou 'spring' (backend Docker)
VITE_API_MODE=spring
VITE_SPRING_API_URL=http://localhost:8080/api
