# Collection Postman

Ce dossier contient la collection Postman pour tester les endpoints de l'API Gateway.

## Import

1. Ouvrir Postman
2. Importer le fichier `depanpro-api.postman_collection.json`
3. Configurer la variable d'environnement `base_url` → `http://localhost:8080/api`
