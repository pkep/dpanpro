global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'spring-gateway'
    metrics_path: '/api/actuator/prometheus'
    static_configs:
      - targets: ['gateway:8080']
        labels:
          application: 'depanpro-gateway'
